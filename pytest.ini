[pytest]
# importlib mode lets test modules with the same basename coexist across plugins
# (e.g. multiple test_storage.py / conftest.py) without sys.path collisions.
addopts = --import-mode=importlib -q
testpaths =
    ci/tests
    plugins
python_files = test_*.py
python_classes = Test*
python_functions = test_*
